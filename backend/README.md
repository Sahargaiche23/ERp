# Backend (Spring Boot)

Services prévus:
- auth-service
- hr-service
- budget-service
- claims-service
- reports-service

Chaque service utilisera Flyway pour les migrations et Spring Security/JWT pour l’auth (via auth-service).
